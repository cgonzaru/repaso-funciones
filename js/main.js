'use strict';

function writeThis (str, maxNumber) {
    for (let i=0; i<maxNumber; i++) {
        console.log(str);
    }
}

writeThis ('Patata', 10);
writeThis ('Aguacate', 7);
writeThis ('Pizza', 50);
